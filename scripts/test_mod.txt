
model{

for(i in 1:n_total){
  TMex[i] ~ dbern(lp[year[i])
  lp[year[i]] ~ dnorm(site_mu, site_tau)
}

site_mu ~ dnorm(0, 0.001) # prior after Kery 2010 (p234)
site_tau <– 1 / (site_sigma * site_sigma)
site_sigma ~ dunif(0, 10)
}
